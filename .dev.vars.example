# Copy to .dev.vars for local wrangler secrets (never commit .dev.vars)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# false = Spotify search + Deezer previews; true/missing creds = mock catalog
MOCK_MODE=true
